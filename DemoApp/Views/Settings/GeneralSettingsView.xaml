<UserControl x:Class="DemoApp.Views.Settings.GeneralSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModels="clr-namespace:DemoApp.ViewModels.Settings"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance viewModels:GeneralSettingsViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="450" 
             d:DesignWidth="800">
    <StackPanel>
        <Border 
            Margin="8"
            Padding="16"
            CornerRadius="4"
            Background="{DynamicResource LayerFillColorDefaultBrush}">

            <DockPanel>
                <ComboBox
                    DockPanel.Dock="Right"
                    Width="200"
                    ItemsSource="{Binding LanguageCollection}"
                    SelectedItem="{Binding SelectedLanguage}">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel
                                Orientation="Horizontal">
                                <Image
                                    Width="24"
                                    Height="24"
                                    Margin="0,0,8,0"
                                    Source="{Binding IconUri}" />
                                <TextBlock
                                    VerticalAlignment="Center"
                                    FontSize="14"
                                    LineHeight="20"
                                    Text="{Binding FullName}" />
                            </StackPanel>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <TextBlock
                    DockPanel.Dock="Left"
                    VerticalAlignment="Center"
                    Text="Language: " />
            </DockPanel>
        </Border>
        
    </StackPanel>
</UserControl>
